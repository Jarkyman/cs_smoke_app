enum ViewState { idle, loading, error, success }

enum Team { t, ct }

enum UtilType { smoke, flash, molotov }

enum VideoType { youtube, instagram, tiktok, other }

enum CreatePinMode { setUtil, setPositions }
